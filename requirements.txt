# Exact pins keep Streamlit Community Cloud rebuilds deterministic. These are
# the versions used by the locally verified Vflat lab environment.
streamlit==1.62.0
pandas==2.2.3
numpy==2.3.5
plotly==6.9.0
scipy==1.17.0
openpyxl==3.1.5
pytest==9.1.1
